[자바스크립트 2]

- 자바스크립트 출력
가장 기본 : alert 사용. 브라우저에서 경고창으로 메시지 띄우기

- 매개 변수
함수의 괄호 안에 들어가는 것

- 문자열
문자를 표현할 때 쓰는 자료의 형태
alert() 함수의 매개 변수로 쓰인 'Hello World!'같은 자료
큰 따옴표, 작은 따옴표 모두 사용가능하지만 일관되게 쓸 필요가 있다.

- 문자열 안에 쓰는 따옴표
내부에 작은 따옴표를 쓰고 싶으면 외부에 큰 따옴표
내부에 큰 따옴표를 쓰고 싶으면 외부에 큰 따옴표

- 조건문에서의 불리언 사용
조건문 괄호 안의 불리언 표현식이 참이면 중괄호 속 문장 실행
거짓이면 중괄호 속 문장 무시

if(불리언 표현식) {
  불리언 표현식이 참일 때 실행할 문장 
}
